apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rag-bot.fullname" . }}-test-connection"
  labels:
    {{- include "rag-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rag-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
